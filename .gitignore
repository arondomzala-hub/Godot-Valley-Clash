# Godot 4+ specific ignores
.godot/
*.translation
export.cfg
# export_presets.cfg is committed on purpose: the Pages deploy workflow needs
# the "Web" preset to run the headless export in CI (no secrets inside).
*.tmp
*.import
.mono/
data_*/
android/

# Colyseus server
node_modules/
server/node_modules/
server/build/
server/.colyseus-cloud/
.colyseus-cloud.json

addons/colyseus/bin/~*

# Local web export output (the Pages workflow builds it in CI)
web_export/

